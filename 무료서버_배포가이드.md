# 🚀 무료 서버에 배포하기 - 완전 초보자 가이드

## 🎯 Hugging Face Spaces 사용하기 (추천!)

**왜 Hugging Face Spaces인가요?**
- ✅ 완전 무료!
- ✅ 신용카드 필요 없음
- ✅ 클릭 몇 번으로 배포 완료
- ✅ 자동으로 공개 URL 제공 (예: https://huggingface.co/spaces/당신이름/face-reading)
- ✅ 친구들과 바로 공유 가능!

---

## 📋 필요한 것

1. **GitHub 계정** 또는 **Google 계정**
2. **Google Gemini API 키** (무료로 받을 수 있어요!)
   - https://aistudio.google.com/app/apikey 에서 발급

---

## 🚀 배포 방법 (10분이면 끝!)

### 1단계: Hugging Face 가입하기

1. https://huggingface.co/ 로 가세요
2. 우측 상단 "Sign Up" 클릭
3. GitHub 계정 또는 Google 계정으로 간편 가입
4. 이메일 인증 완료

### 2단계: 새로운 Space 만들기

1. 로그인 후, 우측 상단 프로필 클릭
2. "New Space" 선택
3. 다음과 같이 설정:
   - **Owner**: 본인 계정 선택
   - **Space name**: `face-reading` (원하는 이름으로)
   - **License**: `MIT` 선택
   - **Select the Space SDK**: `Gradio` 선택
   - **Space hardware**: `CPU basic - Free` (무료!)
   - **Visibility**: `Public` (공개) 또는 `Private` (비공개)

4. "Create Space" 클릭!

### 3단계: 코드 업로드하기

Space가 생성되면, 파일을 업로드해야 해요. 두 가지 방법이 있어요:

#### 방법 1: 웹에서 직접 업로드 (쉬움! 추천!)

1. Space 페이지에서 "Files" 탭 클릭
2. "Add file" > "Create a new file" 클릭

3. **첫 번째 파일**: `app.py`
   - 파일명에 `app.py` 입력
   - `huggingface/app.py` 파일의 내용을 복사해서 붙여넣기
   - "Commit new file to main" 클릭

4. **두 번째 파일**: `requirements.txt`
   - "Add file" > "Create a new file" 다시 클릭
   - 파일명에 `requirements.txt` 입력
   - `huggingface/requirements.txt` 파일의 내용을 복사해서 붙여넣기
   - "Commit new file to main" 클릭

5. **세 번째 파일**: `README.md` (선택사항)
   - 같은 방법으로 `huggingface/README.md` 내용 업로드

#### 방법 2: Git으로 업로드 (개발자용)

```bash
# Space를 클론
git clone https://huggingface.co/spaces/당신이름/face-reading
cd face-reading

# 파일 복사
cp ../huggingface/* .

# 커밋 및 푸시
git add .
git commit -m "Add face reading app"
git push
```

### 4단계: API 키 설정하기 (중요!)

이제 Google Gemini API 키를 안전하게 설정해야 해요:

1. Space 페이지 상단에서 "Settings" 클릭
2. 왼쪽 메뉴에서 "Repository secrets" 선택
3. "New secret" 클릭
4. 다음과 같이 입력:
   - **Name**: `GOOGLE_API_KEY`
   - **Value**: 아까 복사한 Gemini API 키 붙여넣기 (AIza로 시작하는 키)
5. "Add" 클릭!

> 💡 **왜 여기에 설정하나요?**
> API 키를 코드에 직접 넣으면 다른 사람이 볼 수 있어요!
> Secrets에 넣으면 안전하게 보관됩니다.

### 5단계: 빌드 기다리기

1. "App" 탭으로 돌아가세요
2. 화면에 "Building..." 이라고 나올 거예요
3. 1-2분 정도 기다리면... 🎉

### 6단계: 완성! 🎊

- 앱이 실행되면 바로 사용할 수 있어요!
- URL은 `https://huggingface.co/spaces/당신이름/face-reading`
- 이 링크를 친구들에게 공유하세요!

---

## 💡 사용 방법

1. 웹사이트 접속
2. 얼굴 사진 업로드
3. "관상 분석하기" 버튼 클릭
4. 결과 확인!

---

## 🔧 문제 해결

### "GOOGLE_API_KEY가 설정되지 않았습니다" 에러

- Settings > Repository secrets에서 API 키를 제대로 설정했는지 확인
- 키 이름이 정확히 `GOOGLE_API_KEY`인지 확인 (대소문자 구분!)
- Space를 재시작: Settings > "Factory reboot" 클릭

### 앱이 빌드되지 않아요

- "Logs" 탭에서 에러 확인
- `requirements.txt` 파일이 제대로 업로드되었는지 확인
- Python 버전 문제일 수 있음 (Hugging Face는 Python 3.10 사용)

### 사진 업로드가 안 돼요

- 사진 파일 크기 확인 (10MB 이하 권장)
- JPG, PNG 형식인지 확인
- 브라우저를 새로고침해보세요

---

## 🌟 다른 무료 호스팅 옵션들

### Render (백엔드 + 프론트엔드)

**장점:**
- 무료 티어 제공
- FastAPI 앱 그대로 배포 가능
- 자동 HTTPS

**단점:**
- 15분 동안 사용 안 하면 슬립 모드 (첫 접속시 느림)
- 월 750시간 무료 (이후 자동 정지)

**배포 방법:**
1. https://render.com/ 가입
2. "New +" > "Web Service" 선택
3. GitHub 저장소 연결
4. Environment에 API 키 설정
5. Deploy!

### Railway

**장점:**
- 사용하기 쉬움
- GitHub 연동 자동 배포
- 무료 $5 크레딧/월

**단점:**
- 크레딧 소진되면 정지
- 슬립 모드 있음

**배포 방법:**
1. https://railway.app/ 가입
2. "New Project" > "Deploy from GitHub repo"
3. 저장소 선택
4. 환경 변수 설정
5. Deploy!

### Replit

**장점:**
- 코드 편집기 내장
- 실시간 미리보기
- 무료 티어

**단점:**
- 슬립 모드 (사용 안 하면 정지)
- 느릴 수 있음

---

## 📊 비교표

| 플랫폼 | 가격 | 난이도 | 슬립모드 | 추천도 |
|--------|------|--------|----------|---------|
| **Hugging Face Spaces** | 완전 무료 | ⭐ 쉬움 | ❌ 없음 | ⭐⭐⭐⭐⭐ |
| Render | 무료 (제한적) | ⭐⭐ 보통 | ✅ 있음 | ⭐⭐⭐⭐ |
| Railway | $5/월 무료 | ⭐⭐ 보통 | ✅ 있음 | ⭐⭐⭐ |
| Replit | 무료 (제한적) | ⭐ 쉬움 | ✅ 있음 | ⭐⭐⭐ |

---

## 🎉 완료!

이제 여러분의 AI 얼굴 관상 분석 서비스가 전 세계 어디서나 접속 가능합니다!

URL을 친구들에게 공유하고 재미있게 사용해보세요! 🎊

---

## ❓ 추가 도움이 필요하신가요?

- Hugging Face 문서: https://huggingface.co/docs/hub/spaces
- Gradio 문서: https://gradio.app/docs/
- 이슈 제보: https://github.com/juny21c/juny21c/issues

**재미있게 즐기세요!** 🔮✨
